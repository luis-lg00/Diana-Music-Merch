# Copy this file to ".env" and fill in your Stripe TEST keys.
# Get them from: https://dashboard.stripe.com/test/apikeys

# Secret key — server only. NEVER commit or expose in the browser.
STRIPE_SECRET_KEY=REPLACE_WITH_YOUR_STRIPE_TEST_SECRET_KEY

# Optional: change the port (default 4242)
# PORT=4242
